---
target: index.html
total_score: 29
max_score: 32
na_heuristics: 7,10
p0_count: 0
p1_count: 1
timestamp: 2026-08-20T14-20-50Z
slug: index-html
---
# Re-critique: Origin Talent Homepage (index.html)

Method: dual-agent (A: design review · B: detector + browser evidence)

Total: 29/32 applicable (91%) - Excellent. Up from 24/32 (75%, Good) on the first run.

## Verified fixes
- aria-selected/tabindex correctly toggle on tab switch (traced both directions in DOM)
- Fieldset structure confirmed: Client 3 groups, Candidate 4 groups, consent-first ordering confirmed in DOM order
- All 42 field labels have * or (optional), zero gaps
- Form submission: real fetch (no no-cors), response.type="cors" not "opaque", success gated on genuine {"status":"ok"}
- Category link -> role pre-select works correctly live

## Bug found and fixed during this run
Phone pattern regex ^[0-9+ ()-]{7,15}$ threw SyntaxError under browser v-mode regex parsing (unescaped () and - in character class). Fixed to ^[0-9+\(\)\- ]{7,15}$ and verified clean across all 5 occurrences, console clean on submit attempts.

## Priority Issues
[P1] Grouping != chunking - Candidate "Your Details" (10 fields) and both forms' "The Role" (10-11 fields) are still single dense blocks with a heading over them. Fix: split further or move secondary fields behind a disclosure.
[P2] Checklist grid leaves a visibly empty cell - 6 items in 4-col auto-fit grid. Fix: force 3-column grid.
[P2] Consent checkboxes advisory not gating - nothing structurally stops skipping past unchecked boxes into ID Number. Fix: disable Your Details fieldset until consent checked.
[P3] Consent checkbox touch targets ~17-18px, under 24-44px guidance.

## Persona notes
Client: category pre-select works; "The Role" is still an 11-field wall with no sub-priority signal for Duties/Notes.
Candidate: "Before You Start" is genuinely first but scrolls off-screen on mobile before ID Number appears.
Mobile: hamburger fully functional; hero eyebrow orphan at 375px unchanged (out of original fix scope).

## Minor
Required asterisk still matches heading color (mitigated in practice by (optional) tagging). Placeholder contrast ~4.54:1, thin margin over AA.

## Questions
1. Should sensitive fields be technically disabled until consent checked?
2. Is 4 groups with two ~10-field groups a real chunking win or a categorization win?
3. Is the checklist empty cell / flat 10-category scroll the next real risk now that both P0s are closed?
